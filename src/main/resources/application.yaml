spring:
  application:
    name: project
  datasource:
    url: jdbc:postgresql://localhost:5432/sandBox
    username: ${JDBC_USER}
    password: ${JDBC_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true